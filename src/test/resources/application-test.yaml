spring:
  datasource:
    url: jdbc:h2:file:/data/demo
  h2:
    console:
      enabled: true
      path: /h2

camunda:
  bpm:
    admin-user:
      id: admin
      password: admin
    generic-properties:
      properties:
        historyTimeToLive: P1D